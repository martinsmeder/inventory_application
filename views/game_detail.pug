extends layout

block content
  h1 Title: #{game.name}

  p #[strong Console: ]
    a(href=game.console_model.url) #{game.console_model.name}
  p #[strong Description:] #{game.description}
  p #[strong Price:] #{game.price}
  p #[strong Amount in stock:] #{game.number_in_stock}

  hr
  p
  a(href=game.url+'/delete') Delete game
  p
  a(href=game.url+'/update') Update game


